# ============================================================================
#
# ibkrtrader — default values
#
# This is the v0 surface. Most components reference placeholder images
# (registry.local/ibkrtrader/...) that you build yourself. The chart is
# designed to deploy a complete topology even with placeholders so wiring
# can be verified end-to-end.
# ============================================================================

global:
  imageRegistry: ghcr.io/amaliecjensen
  imagePullSecrets:
    - name: ghcr-pull-secret
  storageClass: ""

# ----------------------------------------------------------------------------
# IBKR Gateway
# ----------------------------------------------------------------------------
ibkr:
  # mode selects which gateway StatefulSet strategies point at.
  # Both StatefulSets can be deployed simultaneously by enabling both
  # gateways below; this flag only steers default DNS resolution.
  mode: paper          # paper | live
  account: "DUQ568769"  # IBKR account ID

  # Per-mode gateway configuration. Disable one to save resources.
  gateways:
    paper:
      enabled: true
      image:
        repository: ghcr.io/gnzsnz/ib-gateway
        tag: stable
        pullPolicy: IfNotPresent
      apiPort: 4004
      vncPort: 5900
      # IBC auto-restart time (Toronto) — gateway restarts inside the
      # existing session window so daily 2FA is not re-prompted. The
      # weekly Sunday 01:00 ET reset still requires a manual tap.
      autoRestartTime: "11:55 PM"
      twoFaTimeoutAction: restart
      readOnlyApi: false
      trustedIPs: "10.0.0.0/8"   # pod CIDR — set to "" to restrict to localhost only
      resources:
        requests: { cpu: 250m, memory: 512Mi }
        limits:   { cpu: "2",  memory: 2Gi }
      persistence:
        enabled: true
        size: 5Gi
        accessMode: ReadWriteOnce
      nodeSelector: {}
      tolerations: []
      affinity: {}

    live:
      enabled: false
      image:
        repository: ghcr.io/gnzsnz/ib-gateway
        tag: stable
        pullPolicy: IfNotPresent
      apiPort: 4003
      vncPort: 5900
      autoRestartTime: "11:55 PM"
      twoFaTimeoutAction: restart
      readOnlyApi: false
      resources:
        requests: { cpu: 500m, memory: 1500Mi }
        limits:   { cpu: "2",  memory: 3Gi }
      persistence:
        enabled: true
        size: 5Gi
        accessMode: ReadWriteOnce
      nodeSelector: {}
      tolerations: []
      affinity: {}

  # Credentials. In production replace with externalSecrets or sealed-secrets.
  credentials:
    # If existingSecret is set, the chart will not create a Secret.
    # Expected keys: TWS_USERID, TWS_PASSWORD.
    existingSecret: "ibkrtrader-ibkr-credentials"  # kubectl apply -f ibkr-credentials.secret.yaml
    username: ""
    password: ""

# ----------------------------------------------------------------------------
# Strategies
#
# Each entry produces:
#   - one Deployment (image, resources, env, configmap mount)
#   - one Service (for /metrics scraping)
#   - one ConfigMap (strategy-specific config rendered as YAML)
#
# clientId MUST be unique across all strategies — _helpers.tpl validates this
# at template-render time and fails the install if there are collisions.
# ----------------------------------------------------------------------------
strategies:
  - name: hello
    enabled: true
    image:
      repository: ghcr.io/amaliecjensen/ibkrtrader/strategy-hello
      tag: v0
      pullPolicy: IfNotPresent
    clientId: 11
    replicas: 1
    resources:
      requests: { cpu: 100m, memory: 128Mi }
      limits:   { cpu: 500m, memory: 512Mi }
    config:
      universe: ["AAPL"]
    risk:
      maxOrderNotional: 1000
      maxPosition: 10
      maxDailyLoss: 100
      maxOrdersPerSecond: 1

  - name: nvidia
    enabled: true
    image:
      repository: ghcr.io/amaliecjensen/ibkrtrader/strategy-nvidia
      tag: v0
      pullPolicy: IfNotPresent
    clientId: 12
    replicas: 1
    resources:
      requests: { cpu: 100m, memory: 128Mi }
      limits:   { cpu: 500m, memory: 512Mi }
    config:
      universe: ["NVDA"]
      trade_qty: 1
    risk:
      maxOrderNotional: 5000
      maxPosition: 4
      maxDailyLoss: 500
      maxOrdersPerSecond: 1

# ----------------------------------------------------------------------------
# IBKR Adapter
#
# The adapter owns the single TWS connection (clientId=1 is reserved for it).
# It bridges IBKR ↔ NATS:
#   orders.>        (NATS inbound)  → TWS placeOrder()
#   fills/pnl/pos   (TWS callbacks) → NATS outbound
#   risk.adapter.*  heartbeat + disconnection alerts
# ----------------------------------------------------------------------------
ibkrAdapter:
  enabled: true
  image:
    repository: ghcr.io/amaliecjensen/ibkrtrader/ibkr-adapter
    tag: v0.1.1
    pullPolicy: IfNotPresent
  # clientId=1 is reserved for the adapter. Do NOT use 1 in .strategies[].
  clientId: 1
  resources:
    requests: { cpu: 100m, memory: 128Mi }
    limits:   { cpu: 500m, memory: 256Mi }
  reconnectIntervalS: 10
  logLevel: INFO
  logFormat: json      # json | console
  # Comma-separated tickers to stream realtime market data for.
  # Adapter calls IBKR reqMktData for each symbol and publishes ticks to
  # marketdata.realtime.<SYMBOL>. Must cover every symbol in any enabled
  # strategy's universe; otherwise the strategy receives no bars.
  universe: "NVDA,AAPL"
  nodeSelector: {}
  tolerations: []

# ----------------------------------------------------------------------------
# Risk plane
# ----------------------------------------------------------------------------
riskGateway:
  enabled: true
  image:
    repository: ghcr.io/amaliecjensen/ibkrtrader/risk-gateway
    tag: v0
    pullPolicy: IfNotPresent
  replicas: 2
  port: 8080
  resources:
    requests: { cpu: 100m, memory: 128Mi }
    limits:   { cpu: 500m, memory: 512Mi }

  # Account-wide pre-trade limits. Enforced in addition to per-strategy
  # limits in `.Values.strategies[].risk`.
  global:
    maxAccountDrawdownPct: 0.05
    maxGrossExposure: 1000000
    maxOrdersPerSecond: 50
    restrictedSymbols: []

riskMonitor:
  enabled: true
  image:
    repository: ghcr.io/amaliecjensen/ibkrtrader/risk-monitor
    tag: v0
    pullPolicy: IfNotPresent
  replicas: 2          # leader-elected via Lease
  resources:
    requests: { cpu: 100m, memory: 128Mi }
    limits:   { cpu: 500m, memory: 512Mi }
  # Drift / breach evaluation cadence
  reconcileIntervalSeconds: 10
  # Whether the monitor is allowed to scale strategy Deployments to 0.
  # Disable in dev to avoid accidental kills.
  killSwitchEnabled: true

  # Risk thresholds — passed to the monitor as env vars.
  # Override in values.paper.yaml / values.live.yaml as needed.
  thresholds:
    maxDailyLoss: -500          # halt if daily P&L drops below this (USD)
    maxDrawdownPct: 0.05        # halt at 5% intra-day drawdown from HWM
    maxGrossExposure: 1000000   # halt if gross exposure exceeds this (USD)
    heartbeatTimeoutS: 60       # alert (no halt) if adapter silent > 60s

  # Telegram alerts.
  # Set telegram.enabled=true and provide token + chatId via a secret.
  # Never commit real credentials — use existingSecret in production.
  telegram:
    enabled: false
    existingSecret: ""          # Secret with keys: TELEGRAM_TOKEN, TELEGRAM_CHAT_ID
    token: ""                   # only for local dev; use existingSecret in prod
    chatId: ""

# ----------------------------------------------------------------------------
# API + dashboard
# ----------------------------------------------------------------------------
api:
  enabled: true
  image:
    repository: ghcr.io/amaliecjensen/ibkrtrader/api
    tag: v0
    pullPolicy: IfNotPresent
  replicas: 2
  port: 8000
  resources:
    requests: { cpu: 100m, memory: 128Mi }
    limits:   { cpu: 500m, memory: 512Mi }

dashboard:
  enabled: true
  image:
    repository: ghcr.io/amaliecjensen/ibkrtrader/dashboard
    tag: v0.1.1
    pullPolicy: IfNotPresent
  replicas: 1
  port: 3000
  resources:
    requests: { cpu: 100m, memory: 128Mi }
    limits:   { cpu: 500m, memory: 512Mi }
  ingress:
    enabled: false
    className: ""
    host: trader.example.com
    annotations: {}
    tls: []

# ----------------------------------------------------------------------------
# Data plane
# ----------------------------------------------------------------------------

# CloudNativePG: the operator is installed via the chart dependency.
# `timescale.cluster` configures the per-release Cluster CR.
# Key name is "cloudnative-pg" because the dependency alias removed.
cloudnative-pg:
  enabled: false
  crds:
    create: false

timescale:
  cluster:
    enabled: true
    instances: 1                       # bump to 3 for HA
    image:
      # CNPG-compatible Postgres image with TimescaleDB extension preloaded.
      repository: ghcr.io/clevyr/cloudnativepg-timescale
      tag: "17.5-22"
    storage:
      size: 20Gi
      storageClass: ""
    postgresql:
      parameters:
        max_connections: "200"
    bootstrap:
      database: trader
      owner: trader
    # Plug in S3 backups in v1.
    backup:
      enabled: false

# NATS via subchart. Default config = JetStream enabled, file storage.
nats:
  enabled: true
  config:
    cluster:
      enabled: false                   # bump to 3-node cluster for HA
    jetstream:
      enabled: true
      fileStore:
        enabled: true
        pvc:
          size: 10Gi

# JetStream streams declared in nats-streams.yaml (NACK CRDs, applied
# only if the NACK controller is installed — disabled by default).
jetstreamStreams:
  enabled: false
  bootstrapJob:
    enabled: true
    image: natsio/nats-box:0.16.0

# ----------------------------------------------------------------------------
# Observability
# ----------------------------------------------------------------------------
serviceMonitors:
  # Requires the Prometheus Operator CRDs (kube-prometheus-stack).
  enabled: false
  interval: 30s
  labels: {}

# ----------------------------------------------------------------------------
# RBAC
# ----------------------------------------------------------------------------
rbac:
  create: true

serviceAccount:
  create: true
  name: ""                             # generated from chart name if empty
